# ── Database (Railway auto-sets DATABASE_URL) ──
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/lone_soldier
DATABASE_URL_SYNC=postgresql://postgres:postgres@db:5432/lone_soldier

# ── Redis (Railway auto-sets REDIS_URL) ──
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# ── AI ──
ANTHROPIC_API_KEY=sk-ant-...
LLM_MODEL=claude-sonnet-4-20250514

# ── App ──
APP_ENV=development          # development | production
SECRET_KEY=change-me-in-prod
CORS_ORIGINS=http://localhost:3000

# ── Scanning Agent ──
SCAN_INTERVAL_HOURS=4
MATCH_SCORE_THRESHOLD=0.5

# ── Feature Toggles ──
USE_REAL_SCRAPERS=false
USE_REAL_MAPS=false
USE_REAL_WHATSAPP=false
